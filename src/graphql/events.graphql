query Events {
  events {
    name
    location
    id
  }
}

mutation CreateEvent(
  $name: String!
  $location: String!
  $startDate: String!
  $visible: Boolean!
) {
  createEvent(
    name: $name
    location: $location
    startDate: $startDate
    visible: $visible
  )
}
