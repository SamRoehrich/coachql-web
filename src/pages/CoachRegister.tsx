import React, { FC } from "react";
import RegisterForm from "../components/RegisterForm";

interface Props {}

const CoachRegisterPage: FC<Props> = () => {
  return <RegisterForm />;
};

export default CoachRegisterPage;
